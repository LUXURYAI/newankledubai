export const dynamic = 'force-static';
import type {MetadataRoute} from 'next'; import {languages} from '@/lib/i18n'; import {conditions,procedures} from '@/lib/site-data';
export default function sitemap():MetadataRoute.Sitemap{const base='https://newankledubai.com';const fixed=['','about','medical-director','centers','conditions','procedures','international-patients','insights','faq','contact','book','privacy','terms','cookies','medical-disclaimer'];return [{url:base,lastModified:new Date(),priority:1},...languages.flatMap(lang=>[...fixed.map(p=>({url:`${base}/${lang}/${p}${p?'/':''}`,lastModified:new Date(),priority:p===''?.9:.7})),...conditions.map(c=>({url:`${base}/${lang}/conditions/${c[0]}/`,lastModified:new Date(),priority:.65})),...procedures.map(p=>({url:`${base}/${lang}/procedures/${p[0]}/`,lastModified:new Date(),priority:.65}))])]}
