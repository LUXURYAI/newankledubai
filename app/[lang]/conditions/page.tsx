import Link from 'next/link'; import PageShell from '@/components/PageShell'; import CTA from '@/components/CTA'; import {isLang} from '@/lib/i18n'; import {conditions} from '@/lib/site-data';
export default async function Page({params}:{params:Promise<{lang:string}>}){const {lang:raw}=await params;if(!isLang(raw))return null;const lang=raw;const idx=lang==='en'?1:lang==='ru'?2:3;return <PageShell lang={lang}><main><section className="page-hero"><div className="container narrow"><span className="eyebrow">CONDITIONS</span><h1>{lang==='ru'?'Заболевания стопы и голеностопа':lang==='ar'?'حالات القدم والكاحل':'Foot & Ankle Conditions'}</h1><p className="lead">Clear information and specialist pathways for common, complex and limb-threatening conditions.</p></div></section><section className="section"><div className="container condition-grid">{conditions.map(c=><Link key={c[0]} href={`/${lang}/conditions/${c[0]}/`}><span>→</span><h3>{c[idx]}</h3><p>Symptoms, diagnosis, treatment and recovery.</p></Link>)}</div></section><CTA lang={lang}/></main></PageShell>}
