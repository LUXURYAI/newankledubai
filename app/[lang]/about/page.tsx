import PageShell from '@/components/PageShell'; import CTA from '@/components/CTA'; import {isLang} from '@/lib/i18n'; import {pageCopy,tx} from '@/lib/content';
export default async function Page({params}:{params:Promise<{lang:string}>}){const {lang:raw}=await params;if(!isLang(raw))return null;const lang=raw;const x=pageCopy.about;return <PageShell lang={lang}><main><section className="page-hero"><div className="container narrow"><span className="eyebrow">{tx(x.eyebrow,lang)}</span><h1>{tx(x.title,lang)}</h1><p className="lead">{tx(x.lead,lang)}</p></div></section><section className="section"><div className="container two-col"><div><h2>{tx(x.focusTitle,lang)}</h2></div><div className="prose"><p>{tx(x.p1,lang)}</p><p>{tx(x.p2,lang)}</p></div></div></section><section className="section soft"><div className="container feature-grid">{x.cards.map((c,i)=><article key={i}><b>0{i+1}</b><h3>{tx(c[0],lang)}</h3><p>{tx(c[1],lang)}</p></article>)}</div></section><CTA lang={lang}/></main></PageShell>}
