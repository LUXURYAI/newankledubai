import PageShell from '@/components/PageShell'; import CTA from '@/components/CTA'; import {isLang} from '@/lib/i18n'; import {pageCopy,tx} from '@/lib/content';
export default async function Page({params}:{params:Promise<{lang:string}>}){const {lang:raw}=await params;if(!isLang(raw))return null;const lang=raw;const x=pageCopy.international;return <PageShell lang={lang}><main><section className="page-hero"><div className="container narrow"><span className="eyebrow">{tx(x.eyebrow,lang)}</span><h1>{tx(x.title,lang)}</h1><p className="lead">{tx(x.lead,lang)}</p></div></section><section className="section"><div className="container"><div className="section-heading"><h2>{tx(x.supportTitle,lang)}</h2></div><div className="card-grid">{x.cards.map((c,i)=><article className="service-card" key={i}><span className="service-icon">0{i+1}</span><h3>{tx(c[0],lang)}</h3><p>{tx(c[1],lang)}</p></article>)}</div></div></section><section className="section journey"><div className="container"><div className="journey-steps">{x.journey.map((s,i)=><div key={i}><span>{String(i+1).padStart(2,'0')}</span><p>{tx(s,lang)}</p></div>)}</div></div></section><CTA lang={lang}/></main></PageShell>}
