import Link from 'next/link'; import PageShell from '@/components/PageShell'; import CTA from '@/components/CTA'; import {isLang} from '@/lib/i18n'; import {procedures} from '@/lib/site-data';
export default async function Page({params}:{params:Promise<{lang:string}>}){const {lang:raw}=await params;if(!isLang(raw))return null;const lang=raw;const idx=lang==='en'?1:lang==='ru'?2:3;return <PageShell lang={lang}><main><section className="page-hero"><div className="container narrow"><span className="eyebrow">SURGICAL PROCEDURES</span><h1>{lang==='ru'?'Современная хирургия стопы и голеностопа':lang==='ar'?'جراحة متقدمة للقدم والكاحل':'Advanced Foot & Ankle Surgery'}</h1><p className="lead">Personalized surgical options designed around diagnosis, function, recovery goals and long-term outcomes.</p></div></section><section className="section"><div className="container condition-grid">{procedures.map(c=><Link key={c[0]} href={`/${lang}/procedures/${c[0]}/`}><span>→</span><h3>{c[idx]}</h3><p>Indications, planning, procedure and recovery.</p></Link>)}</div></section><CTA lang={lang}/></main></PageShell>}
