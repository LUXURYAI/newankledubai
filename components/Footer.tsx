import Link from 'next/link'; import type {Lang} from '@/lib/i18n'; import {nav,t,copy} from '@/lib/site-data'; import {ui,tx} from '@/lib/content';
export default function Footer({lang}:{lang:Lang}){return <footer><div className="container footer-grid"><div><div className="brand footer-brand"><span className="brand-box">NAD</span><span><b>New Ankle Dubai</b><small>{tx(ui.siteTagline,lang)}</small></span></div><p>{tx(ui.disclaimer,lang)}</p></div><div><h4>{tx(ui.explore,lang)}</h4>{nav.slice(0,6).map(n=><Link key={n.href} href={`/${lang}/${n.href}/`}>{t(n.label,lang)}</Link>)}</div><div><h4>{tx(ui.contact,lang)}</h4><Link href={`/${lang}/book/`}>{copy[lang].book}</Link><Link href={`/${lang}/international-patients/`}>{tx(ui.international,lang)}</Link><span>{tx(ui.location,lang)}</span><a href="mailto:info@newankledubai.com">info@newankledubai.com</a></div></div><div className="container footer-bottom">© 2026 New Ankle Dubai. {tx(ui.rights,lang)}</div></footer>}
